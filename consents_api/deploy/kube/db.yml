---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: consents-api
  name: consents-api-db
spec:
  selector:
    matchLabels:
      app: consents-api-db
  template:
    metadata:
      labels:
        app: consents-api-db
    spec:
      containers:
        - name: database
          image: postgres:16.3-bullseye
          resources:
            limits:
              memory: "300Mi"
              cpu: "200m"
          env:
            - name: POSTGRES_PASSWORD
              value: "consents_api"
            - name: POSTGRES_USER
              value: "consents_api"
            - name: POSTGRES_DB
              value: "consents_api"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  namespace: consents-api
  name: "consents-api-db-service"
spec:
  selector:
    app: consents-api-db
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: batch/v1
kind: Job
metadata:
  namespace: consents-api
  name: consents-api-migrator
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
      - name: migrator
        image: consents_api:latest
        command:
          - "alembic"
          - "upgrade"
          - "head"
        resources:
          limits:
            memory: "200Mi"
            cpu: "250m"
        env:
          - name: "CONSENTS_API_DB_HOST"
            value: "consents-api-db-service"
      initContainers:
        - name: wait-for-db
          image: toschneck/wait-for-it:latest
          command: ["./wait-for-it.sh", "-t", "60", "consents-api-db-service:5432"]
      restartPolicy: Never

---
